module.exports = {
    preset: 'ts-jest',
    testEnvironment: 'node',
    testMatch: ['**/FizzBuzz/*.test.js', '**/PalindromeNumber/*.test.js'], // Adjust the path to match your directory structure
};